apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: users-devsecops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: users_lab
            - name: POSTGRES_USER
              value: users_app
            - name: POSTGRES_PASSWORD
              value: bd1234
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: users-devsecops
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432