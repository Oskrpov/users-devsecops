apiVersion: kind.x-k8s.io/v1alpha4
kind: Cluster

name: users-devsecops

nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080
        hostPort: 5500
        protocol: TCP

  - role: worker

  - role: worker